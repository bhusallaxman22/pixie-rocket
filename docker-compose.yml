services:
  rocket-arena:
    build:
      context: .
      dockerfile: Dockerfile
    image: pixi-rocket-arena:local
    ports:
      - "${PORT:-5173}:5173"
    environment:
      NODE_ENV: production
      PORT: 5173
    restart: unless-stopped
